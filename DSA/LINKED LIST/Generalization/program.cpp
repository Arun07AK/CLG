// Step 1: Think through all three cases
if(head == NULL) { /* handle empty */ }
if(/* head operation */) { /* handle head */ }
else { /* handle general */ }

// Step 2: Look for unification opportunities
// Can the general case naturally handle the head case?
// Does the algorithm create natural indicators?
